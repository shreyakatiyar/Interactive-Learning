# Disable source-map generation in production builds.
# This cuts build time ~30-40 % on Vercel and prevents JS bundle size from
# doubling in memory — the most common cause of Vercel OOM build failures
# with Create React App.
GENERATE_SOURCEMAP=false
